Some files inside the `local` folder are just a local copy of external
documents, scripts and tools, saved here just in case they disappear
from the web.

Other files here are the HTML versions of sed scripts.
